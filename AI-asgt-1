#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;

struct point{
	float x;
	float y;
};

void trail(vector<ll> sequence,int n){
	for(ll i=0;i<n;i++){
		cout<<sequence[i]<<" ";
	}
	cout<<endl;
}
void createtour(vector<vector<float>> V,ll n){
	ll count = 0;
	ll i = (rand()%n);
	bool visited[n] = 0;
	vector<ll> sequence (n);
	sequence[count] = i; 
	while(count < n){
		float min = V[i][(i+1)%n];
		for(int i = 
	}
	
}


int main(){
	
	ll n;
	string iseuclidean;
	cin>>iseuclidean>>n;
	vector<point> cities(n);
	for(ll i = 0;i<n;i++){
		cin>>cities[i].x>>cities[i].y;
	}
	vector<vector<float>> dist(n,vector<float>(n));
	for(ll i =0;i<n;i++){
		for(ll j=0;j<n;j++){
			cin>>dist[j][i];
		}
	}
	createtours(dist,n);
	return 0;
}

